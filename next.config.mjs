/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode:false,
    images:{
        domains:['lh3.googleusercontent.com', 'api.dicebear.com', 'avatars.githubusercontent.com', 'images.unsplash.com']
    }
};

export default nextConfig;
